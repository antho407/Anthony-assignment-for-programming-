//a program to determine weather a number is even or odd

#include<stdio.h>
int main ()
{
int number;
printf("enter the number");
scanf("%d",&number);
if(number%2 ==0)
{printf(" %d is even",number);
}
else
{
printf("%d is old number",number);
}

return 0;
}